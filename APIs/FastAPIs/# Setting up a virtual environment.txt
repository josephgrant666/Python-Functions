# Setting up a virtual environment 

# 1. Select a terminal within the VS Code window
# 2. Type py -3 -m venv (name of venv) -> this then creates a new folder 
# 3. Open command palette -> select interpreter -> then select the path that leads to the python.exe file inside of the venv folder. 